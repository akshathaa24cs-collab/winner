# Frontend boundary

The React/Vite application currently lives at the repository root (`src/`) for compatibility with the existing Vite setup. This folder is reserved for frontend-specific assets, API clients, and future feature modules as the UI grows.
